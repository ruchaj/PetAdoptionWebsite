<!DOCTYPE html>
<html>

<style>
    body {
        margin: 0;
        padding: 0;
        background-color: #f1f1f1;
    }

    h1 {
        text-align: center;
        font-size: 36px;
        margin: 20px 0;
        color: #333;
    }

    form {
        border: 2px solid #ccc;
        border-radius: 10px;
        padding: 20px;
        margin: 20px auto;
        max-width: 500px;
        background-color: #fff;
        text-align: left;
    }

    label {
        display: block;
        margin-bottom: 10px;
        font-size: 18px;
        font-weight: bold;
        color: #333;
    }

    input[type="radio"] {
        margin-right: 10px;
    }

    button[type="submit"] {
        background-color: #4CAF50;
        color: white;
        padding: 10px 20px;
        border: none;
        border-radius: 5px;
        font-size: 18px;
        cursor: pointer;
    }

        button[type="submit"]:hover {
            background-color: #45a049;
        }

    #result {
        font-size: 24px;
        font-weight: bold;
        color: #333;
        margin-top: 20px;
    }
</style>
<title>Pet Adoption Survey</title>
<script>
    function submitForm(event) {
        event.preventDefault(); // Prevent page refresh on form submission

        // Get the values of the form fields
        var size = document.querySelector('input[name="size"]:checked').value;
        var energyLevel = document.querySelector('input[name="energyLevel"]:checked').value;
        var climate = document.querySelector('input[name="climate"]:checked').value;
        var lifestyle = document.querySelector('input[name="lifestyle"]:checked').value;

        // Determine the best pet based on the user's answers
        var bestPet;
        if (size === 'small' && energyLevel === 'low' && climate === 'warm' && lifestyle === 'quiet') {
            bestPet = 'Cat';
        } else if (size === 'medium' && energyLevel === 'low' && climate === 'temperate' && lifestyle === 'quiet') {
            bestPet = 'Hamster';
        } else if (size === 'large' && energyLevel === 'low' && climate === 'cold' && lifestyle === 'quiet') {
            bestPet = 'Fish';
        } else if (size === 'small' && energyLevel === 'high' && climate === 'warm' && lifestyle === 'active') {
            bestPet = 'Bird';
        } else if (size === 'medium' && energyLevel === 'high' && climate === 'temperate' && lifestyle === 'active') {
            bestPet = 'Dog';
        } else if (size === 'large' && energyLevel === 'high' && climate === 'cold' && lifestyle === 'active') {
            bestPet = 'Reptile';
        }else{
            bestPet = '';

        }

        // Display the best pet to the user, or an error message.
        if(bestPet != ''){
            document.getElementById('result').textContent = 'Your best pet is a ' + bestPet + '!';
        }
        else{
            document.getElementById('result').textContent = 'Unfortunately, we cannot find the best pet for you based on your responses. You can try again, or check out our other options!';
        }

        // Reset the form
        event.target.reset();
    }
</script>


<h1>Pet Adoption Survey</h1>
<form onsubmit="submitForm(event)">
  <!--Adds a form of radio options for each category. Clicking on them will register with the JavaScript once submit is pressed, and it takes the information to configure the best pet response.-->
    <h2>What size pet are you looking for?</h2>
    <label for="small">
        <input type="radio" id="small" name="size" value="small" required> Small (under 20 lbs)
    </label><br>
    <label for="medium">
        <input type="radio" id="medium" name="size" value="medium"> Medium (20-50 lbs)
    </label><br>
    <label for="large">
        <input type="radio" id="large" name="size" value="large"> Large (over 50 lbs)
    </label><br>

    <h2>What energy level are you looking for?</h2>
    <label for="low">
        <input type="radio" id="low" name="energyLevel" value="low" required> Low (couch potato)
    </label><br>
    <label for="high">
        <input type="radio" id="high" name="energyLevel" value="high"> High (active)
    </label><br>

    <h2>What climate do you live in?</h2>
    <label for="warm">
        <input type="radio" id="warm" name="climate" value="warm" required> Warm
    </label><br>
    <label for="temperate">
        <input type="radio" id="temperate" name="climate" value="temperate"> Temperate
    </label><br>
    <label for="cold">
        <input type="radio" id="cold" name="climate" value="cold"> Cold
    </label><br>

    <h2>What is your lifestyle like?</h2>
    <label for="quiet">
        <input type="radio" id="quiet" name="lifestyle" value="quiet" required> Quiet (homebody)
    </label><br>
    <label for="active">
        <input type="radio" id="active" name="lifestyle" value="active"> Active (always on the go)
    </label><br>

    <button type="submit">Submit</button>
    <p id="result"></p>

</form>


</html>
