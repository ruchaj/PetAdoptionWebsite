@page
@model ContosoCrafts.WebSite.Pages.AllPetsListingModel
@{
    ViewData["Title"] = "Pet Adoption";
}

<!DOCTYPE html>

<html>
    <head>
        <style>
            table {
                width: 100%
            }
            td, th {
                padding: 8px;
            }
        </style>
    </head>
<header>
    <h1>All available pets:</h1>
</header>

<table>
    <thead>
        <tr>
            <th>Name</th>
            <th>Breed</th>
            <th>Location</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        
        @foreach (var pet in Model.Pets)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => @pet.Name)</td>
                <td>@Html.DisplayFor(modelItem => @pet.Breed)</td>
                <td>@Html.DisplayFor(modelItem => @pet.Location)</td>
                <td><a asp-page="Read" asp-route-id="@pet.Id">Read</a></td>
            </tr>
        }
       
    </tbody>
</table>
</html>


